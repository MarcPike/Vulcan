using NUnit.Framework;

namespace DAL.Vulcan.NUnit.Tests.Users
{
    [TestFixture]
    public class ReCreateUserRoles
    {
        //[TestMethod]
        //public void CreateAllIndexes()
        //{
        //    var securityManager = new SecurityManager().ForApplication("vulcancrm");

        //    foreach (var role in securityManager.Application.Roles)
        //    {
        //        foreach (var roleMember in role.Role.RoleMembers)
        //        {
        //            if (role.Role.Name == "SalesPerson")
        //            {
        //                var salesPerson = 
        //            }
        //        }
        //    }
        //}
    }
}
